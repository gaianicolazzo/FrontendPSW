import 'package:flutter/material.dart';

class Stationery extends StatelessWidget {
  const Stationery({super.key});


  @override
  Widget build(BuildContext context) {
      return Scaffold(
        appBar: AppBar(
          title: const Text('Cancelleria')));
          //TODO
  }
}