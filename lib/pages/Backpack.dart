import 'package:flutter/material.dart';

class Backpack extends StatelessWidget {
  const Backpack({super.key});


  @override
  Widget build(BuildContext context) {
      return Scaffold(
        appBar: AppBar(
          title: const Text('Zaini e borsellini')));
          //TODO
  }
}


